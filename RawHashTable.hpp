/* c++ std::unordered_map is a standard hashtable used YET,
make a hashtable for giggles 
Amortized TC O(1) 
includes hash function to create keys
insert, search, delete values
Collision can occur but like who cares */

class HashTable:
{
public:
  HashTable();
  ~HashTable();
};
